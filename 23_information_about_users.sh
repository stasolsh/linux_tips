#!/bin/bash
id                      # Listing the current information of a user
last                    # Listing the last time users have logged into the system
w                       # Listing only the active logins on the system