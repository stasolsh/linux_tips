#!/bin/bash
ifconfig                # Display network interfaces and their configurations
ip addr show            # Display IP addresses and network interfaces
ip link show            # Displays a list of all the available network interfaces and their link layer addresses
ip route show           # Displays a current IPv4 routing table